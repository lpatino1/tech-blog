<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TechBlog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">

    <nav class="display-flex justify-space-between align-center navbar">
      <div>
        <h1>
          <a id="navbarHeader" href="/">TechBlog</a>
        </h1>
      </div>

      <div >
        {{#if logged_in}}
        <a class="navbarlinks" href="/profile">Dashboard</a>
        <a class="navbarlinks" href="/">Home</a>
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a class="navbarlinks" href="/profile">Dashboard</a>
        <a class="navbarlinks" href="/">Home</a>
        <a class="navbarlinks" href="/login">Login</a>
        {{/if}}
      </div>
      <!-- Conditionally render login or logout links -->

    </nav>

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>